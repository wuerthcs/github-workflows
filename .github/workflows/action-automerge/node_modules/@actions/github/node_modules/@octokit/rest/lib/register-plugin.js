module.exports = registerPlugin;

const factory = require("@octokit/rest/lib/factory");

function registerPlugin(plugins, pluginFunction) {
  return factory(
    plugins.includes(pluginFunction) ? plugins : plugins.concat(pluginFunction)
  );
}
