import { VERSION } from "@octokit/plugin-paginate-rest/dist-src/version";
import { paginate } from "@octokit/plugin-paginate-rest/dist-src/paginate";
import { iterator } from "@octokit/plugin-paginate-rest/dist-src/iterator";
import { composePaginateRest } from "@octokit/plugin-paginate-rest/dist-src/compose-paginate";
import {
  isPaginatingEndpoint,
  paginatingEndpoints
} from "@octokit/plugin-paginate-rest/dist-src/paginating-endpoints";
function paginateRest(octokit) {
  return {
    paginate: Object.assign(paginate.bind(null, octokit), {
      iterator: iterator.bind(null, octokit)
    })
  };
}
paginateRest.VERSION = VERSION;
export {
  composePaginateRest,
  isPaginatingEndpoint,
  paginateRest,
  paginatingEndpoints
};
