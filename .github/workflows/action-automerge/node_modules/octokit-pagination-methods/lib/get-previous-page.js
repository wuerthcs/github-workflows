module.exports = getPreviousPage

const getPage = require('octokit-pagination-methods/lib/get-page')

function getPreviousPage (octokit, link, headers) {
  return getPage(octokit, link, 'prev', headers)
}
